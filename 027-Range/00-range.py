#create a sequence using range function
#
#In order to output the range as a list,
#we need to explicitly convert it to a 
#list, using the "list()" function.
numbers = list(range(10))
print(numbers)